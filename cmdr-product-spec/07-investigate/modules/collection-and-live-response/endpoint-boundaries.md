# Endpoint Agent boundaries

Endpoint Agent observes, collects, executes authorized local operations, exposes health/capabilities and produces local results. Investigate prepares context and interprets outputs. Endpoint Agent owns no Case, Evidence, Finding, Decision, Response Run or workflow métier. No protocol, command syntax, transport, PKI, port or engine is selected here.
