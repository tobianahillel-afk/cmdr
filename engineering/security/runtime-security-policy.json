{
  "schema_version": 1,
  "default_policy": "deny-unregistered-runtime-boundary",
  "global_coverage_floor_percent": 80,
  "changed_security_critical_coverage_floor_percent": 90,
  "authorization_gate_id": "SEC-AUTH-NEG-001",
  "tenant_isolation_gate_id": "SEC-TENANT-ISO-001",
  "scopes": [
    {
      "boundary_id": "pilot-context-envelope-runtime",
      "owner": "E9-PILOT-001C",
      "security_critical_paths": [
        "product-runtime/context-envelope/**"
      ],
      "authorization_required": true,
      "authorization_rationale": "The pilot contract carries an explicit destination authorization outcome and permission-denied state; executable runtime must prove authorization-negative behavior before acceptance.",
      "tenant_isolation_required": true,
      "tenant_isolation_rationale": "The pilot projects tenant/environment references across context transitions; executable runtime must prove cross-tenant isolation and reference clearing before acceptance."
    },
    {
      "boundary_id": "event-search-runtime",
      "owner": "E10-INV-002",
      "security_critical_paths": [
        "product-runtime/event-search/**"
      ],
      "authorization_required": true,
      "authorization_rationale": "CAP-INV-002 requires server-side search execution permission checks; the boundary must prove permission-denied behavior before runtime acceptance.",
      "tenant_isolation_required": true,
      "tenant_isolation_rationale": "CAP-INV-002 Search is single-selected-Tenant with no silent fallback; the boundary must prove missing, wildcard and cross-tenant scopes fail closed."
    },
    {
      "boundary_id": "event-search-frontend-runtime",
      "owner": "E10-INV-002F",
      "security_critical_paths": [
        "product-runtime/event-search-frontend/**"
      ],
      "authorization_required": true,
      "authorization_rationale": "INV-EVS-001 can render protected search/event state but server authorization remains authoritative; frontend runtime must prove permission-denied rendering never exposes protected content before acceptance.",
      "tenant_isolation_required": true,
      "tenant_isolation_rationale": "Event Search deep-link and visible context carry tenant/environment scope; frontend runtime must prove malformed or cross-tenant client state cannot silently widen the selected tenant context."
    },
    {
      "boundary_id": "event-inspection-runtime",
      "owner": "E10-INV-004",
      "security_critical_paths": [
        "product-runtime/event-inspection/**"
      ],
      "authorization_required": true,
      "authorization_rationale": "CAP-INV-004 exposes permission-gated Telemetry Event projections with distinct raw/rendered outcomes; executable runtime must prove denied access returns no protected projection and raw-denied paths leak no raw or raw-derived sensitive content.",
      "tenant_isolation_required": true,
      "tenant_isolation_rationale": "CAP-INV-004 inspection and pivot context preserve tenant/environment/event identity; executable runtime must prove missing, wildcard and cross-tenant source or pivot context fails closed with no reference widening."
    }
  ]
}
